services:
  - type: web
    name: socialmedia-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python3 manage.py migrate && gunicorn socialmedia_backend.wsgi:application --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0